using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Animations;

public class Broomisator : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //transform.position = GameObject.Find("VR camera").transform.position;
        //GameObject.Find("VR camera").GetComponent<PositionConstraint>().constraintActive = true;
        //GameObject.Find("VR camera").GetComponent<PositionConstraint>().enabled = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
